<Window x:Class="View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:View"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Window.Resources>
        <LinearGradientBrush x:Key="brush" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="Gray" Offset="0" />
            <GradientStop Color="White" Offset="0.5" />
            <GradientStop Color="Black" Offset="1" />
        </LinearGradientBrush>
    </Window.Resources>
    
    <StackPanel>
        <TextBlock Text="Celsius" Background="{StaticResource brush}" Padding="2 2" FontWeight="Bold" FontSize="16"/>
        <TextBox x:Name="celsiusBox" FontSize="16" TextAlignment="Center"/>
        
        <TextBlock Text="Fahrenheit" Background="{StaticResource brush}" Padding="2 2" FontWeight="Bold" FontSize="16"/>
        <TextBox x:Name="fahrenheitBox" FontSize="16" TextAlignment="Center"/>
        
        <Button Content="Convert to Celsius" Click="ConvertToCelsius"/>
        <Button Content="Convert to Fahrenheit" Click="ConvertToFahrenheit"/>
    </StackPanel>
   
</Window>
